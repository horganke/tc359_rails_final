<%= form_for(@character) do |f| %>
  <% if @character.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@character.errors.count, "error") %> prohibited this character from being saved:</h2>

      <ul>
      <% @character.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :powers %><br>
    <%= f.text_area :powers %>
  </div>
  <div class="field">
    <%= f.label :first_appearance %><br>
    <%= f.date_select :first_appearance %>
  </div>
  <div class="field">
    <%= f.label :good %><br>
    <%= f.check_box :good %>
  </div>
  <div class="field">
    <%= f.label :evil %><br>
    <%= f.check_box :evil %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
